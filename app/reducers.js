/**
 * Created by v on 1/15/17.
 */
import {combineReducers} from 'redux';
const testCard = (state = {},action)=>{
	switch (action.type){
		default:
			return state
	}
};
let appReducer = combineReducers({
	testCard
})
export default appReducer;
